machine:
  node:
    version: 6.11.0
compile:
  override:
    - npm install
    - cd api && npm install
    - cd web && npm install
deployment:
  production:
    branch: master
    commands:
      - ./bin/deploy.sh
